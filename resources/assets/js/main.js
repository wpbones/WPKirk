/**
 * Javascript sample.
 *
 * You can use this Javascript as start for your project.
 *
 */

(function($)
{

  "use strict";

  function openAlert()
  {
    alert( "Hi there, the jQuery version is " + $().jquery );
  }

  openAlert();

})(jQuery);